# Boss Acceptance Checklist

只验收 4 件事，页面地址：

```text
http://23.81.118.51:18080/
```

如果先出现访问密码页，由维护人先完成一次解锁，再继续下面步骤。

## 1. 首屏状态

- 打开页面后，不应是白屏、报错页或永久 loading
- 顶部应能看到更新时间、刷新状态、订阅计划这类状态信息
- 首页主体应正常出现卡片和分区

## 2. 运行会话

- 在“总览”里看 `当前 Codex 线程`，应有数值，不能一直是 `-`
- 在“总览”里看 `当前模型`，应有模型名，或至少有当前会话费用提示
- 需要二次确认时，进“全部会话与账单” -> “全部会话” -> “性能”，确认最近会话状态能看到 `进行中` / `已完结` / `已中断`

## 3. 今日费用

- 首页“首页关键卡片”里直接看 `今日花费`
- 数值可为 `￥0` / `$0` 级别，但不能空白、`NaN` 或报错

## 4. 5h / 7d 限额

- 在“限制窗口”区域确认能看到 `5小时窗口` 和 `7天窗口`
- 两张卡都应展示：
  - 当前使用百分比
  - `剩余 xx%`
  - 重置时间
- 同一区域的“账户限制信息”应能看到当前计划

## 通过标准

- 上面 4 项都可见且可解释，就算验收通过
- 只要出现白屏、无会话、今日费用空白、或 5h/7d 窗口缺失，就算未通过

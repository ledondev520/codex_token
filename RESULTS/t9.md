# T9 Results

## Outcome

Implemented the requested dashboard refinement round:

- USD values now render with two decimal places.
- Pricing rows are grouped when unit prices match.
- `gpt-5.4-codex` is displayed as `GPT-5.4`.
- Recent sessions and billing trends now share one tabbed section.
- Token and cost trends now share one hoverable combined chart.
- The top area now shows the subscription plan.
- Limit windows now show reset progress percentages and progress bars.
- Users can switch the scanned local Codex folder from the UI through `POST /api/source`.

## Verification

- `npm test`
  - Result: 7 passing tests on 2026-03-08 18:37 Asia/Shanghai
- Playwright smoke test with fixture-backed local servers
  - Verified top plan badge (`PRO`)
  - Verified merged pricing row (`gpt-5-codex / gpt-5.1-codex / gpt-5.1-codex-max`)
  - Verified `GPT-5.4` label in current model, pricing table, and recent sessions
  - Verified tab switch from recent sessions to billing view
  - Verified hover tooltip text includes both token M and USD cost

## Notes

- Browser-based absolute folder picking is not available in plain local HTTP, so the UI accepts a folder path input and validates it server-side.

# T19 Result

## Outcome

The temporary VPS preview endpoint no longer requires a username or password.

Preview URL:

```text
http://23.81.118.51:18080/
```

## Deliverables

- VPS nginx site on `18080` updated to remove Basic Auth
- obsolete htpasswd file removed from the VPS
- checkpoint documentation updated to reflect the unauthenticated state

## Validation

- `curl http://23.81.118.51:18080/`
- `npm test`

## Notes

- This makes the preview materially less private. Anyone who can reach the IP and port can load the dashboard.
- Because the endpoint is still plain HTTP, traffic is not encrypted in transit.

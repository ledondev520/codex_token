# T43 Result - Homepage Runtime Verification Backfill

## Scope
- Backfill homepage verification for the refactored first screen.
- Cover `运行状态` / `运行会话列表` / `今日费用` / `5小时限额` / `7天限额`.
- Add abnormal-state and quota ETA wording coverage.
- Keep prompt detail accordion access covered.

## Delivered
- Added `describeRateLimitWindow`, `estimateRateLimitExhaustion`, `describeRuntimeStatus`, and `getRuntimeSessionRows` to `src/lib/dashboard-logic.mjs`.
- Added a compact homepage runtime overview section in `src/App.jsx` with:
  - `运行状态`
  - `运行会话列表`
  - `今日费用`
  - `5小时限额`
  - `7天限额`
  - explicit `估算触顶时间` copy
- Extended `server/lib/usageRepository.js` so `snapshot.live.currentSession` carries `threadId`, `statusLabel`, `title`, and `titlePreview`, which the homepage now uses directly.
- Expanded regression coverage in `test/dashboardView.test.js` and `test/usageRepository.test.js`.

## Verification
- `node --test test/dashboardView.test.js` -> PASS (33 passing)
- `npm test` -> PASS (46 passing)
- `npm run build` -> PASS

## Notes
- `估算触顶时间` is intentionally heuristic and based on linear consumption within the current window; the real reset timestamp remains visible as the authoritative limit boundary.

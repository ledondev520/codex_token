# T22 Result

## Outcome

This round focused on the issues visible in the user screenshots and mobile view:

- The OpenClaw model-fee block now renders as a real two-column table instead of awkward div rows.
- Mobile view now uses a top-level source tab switcher so `Codex 本地` and `OpenClaw / OAuth` are not stacked into one long, sparse page.
- Card padding, value sizing, and mobile spacing were tightened to improve information density and reduce overflow.
- Residual “design process” helper copy was removed from the page.
- Session detail now lists each captured user turn from local event logs in chronological order.
- The live snapshot service was hardened so switching the source directory cannot be overwritten by an older background refresh result.

## Validation

- `npm test` -> 27 passing
- Playwright on `http://127.0.0.1:4329/`:
  - mobile source tabs switch correctly
  - OpenClaw fee summary renders as a table
  - session detail shows prompt summary plus per-turn user messages
  - mobile and desktop layouts both render with live data

## Notes

- User-turn history currently depends on local `event_msg.user_message` availability. That limitation is recorded as `R15` in `RISKS.md`.

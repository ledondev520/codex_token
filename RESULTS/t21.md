# T21 Result

## Outcome

The dashboard now separates two token-consumption paths:

- `Codex 本地`: direct usage read from `~/.codex` SQLite + session JSONL
- `OpenClaw / OAuth`: indirect Codex usage read from `codexbar cost --provider codex`, with session rows heuristically tagged when their local `cwd` lives under `~/.openclaw`

The sessions table includes a new `来源` column and `来源筛选` control, the live-session summary includes the source label, long thread titles are summarized in-table, and session detail prompts now show a concise summary with an expandable full prompt instead of dumping the entire text immediately.

The visual system was also flattened further toward shadcn/ui conventions: simpler cards, badge/tabs semantics, reduced decorative styling, and clearer information hierarchy.

## Validation

- `npm test` -> 26 passing
- Playwright smoke on `http://127.0.0.1:4329/` -> verified source badges, source filter, summarized titles, expandable prompt details, and mobile/desktop rendering

## Notes

- The OpenClaw/OAuth session tag is based on current local thread metadata heuristics (`cwd` under `~/.openclaw`). This is recorded as `R14` in `RISKS.md`.

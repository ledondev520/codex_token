# T12 Results

## Outcome

Removed cold-start blocking from the first dashboard request.

- The live snapshot service now returns a cached or placeholder snapshot immediately.
- Fast warmup and full refresh now run asynchronously outside the request path.
- Server creation primes the snapshot service before the first browser request arrives.

## Verification

- `npm test`
  - Result: 10 passing tests on 2026-03-08 19:24 Asia/Shanghai
- Latency spot-check on a fresh instance at `http://127.0.0.1:4327`
  - `GET /` took about `0.09s`
  - first `GET /api/snapshot` took about `0.006s`

## Notes

- Full history parsing still exists and can consume time in the background, but it no longer blocks first paint.

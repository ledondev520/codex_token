# T46 Result

## Outcome

- Replaced the lower `限制窗口` section with `限额补充信息`.
- Removed repeated 5-hour / 7-day usage cards and duplicate account-limit copy.
- Kept only the secondary information that still adds value:
  - reset-rhythm progress
  - current-session billing detail
  - pricing-page entry
- Deleted dead homepage code: `HomepageRuntimeOverview`, `RuntimeLimitCard`, and the duplicated `buildResetCopy()` path.

## Evidence

- Regression tests: `node --test test/dashboardView.test.js`
- Build: `npm run build`
- Full suite: `npm test`
- Browser smoke: Playwright snapshot on `http://127.0.0.1:4329/`
- Screenshot: `/Users/helena/Cursor/codex_token/output-playwright-home-optimized-top.png`

## Notes

- The first screen remains the single place for quota usage percentage, remaining percentage, reset copy, and ETA copy.
- The lower section now behaves as a true detail layer instead of a second summary layer.

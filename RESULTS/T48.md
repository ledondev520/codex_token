# T48 Result

## Outcome

- Rebuilt the homepage into two ledgers:
  - `我直接使用 Codex`
  - `小龙虾代用`
- Removed the old separate right-side OpenClaw summary panel.
- Kept the top layer focused on:
  - 今日费用
  - 今日 Token
  - 近 7 天费用
  - 近 7 天 Token
  - one state metric per ledger
- Running session cards on the homepage now open the existing detail dialog directly.
- The workbench now follows the selected ledger and no longer mixes the two sources by default.
- Advanced analysis and configuration moved under `高级明细`.

## Evidence

- Regression tests: `node --test test/dashboardView.test.js`
- Full verification: `npm test`
- Browser verification: Playwright browser snapshot on `http://127.0.0.1:4329/`
- Interaction verification: opened a top-level running session card and confirmed `会话详情` dialog appears

## Notes

- The stale 5-hour quota label now uses `快照过期` instead of the more ambiguous `等待刷新`.
- The detail dialog source badge was aligned with the new ledger naming to avoid switching back to old technical labels after click-through.

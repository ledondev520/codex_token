# T15 Results

## Outcome

Installed a fixed-port background service for the dashboard on `127.0.0.1:4329`.

- Added a repo-local launch script: `scripts/run-dashboard.sh`
- Added a repo-local LaunchAgent plist: `launchd/com.helena.codex-usage-dashboard.plist`
- Bootstrapped it with `launchctl` under the current macOS user session

## Verification

- `launchctl print gui/501/com.helena.codex-usage-dashboard`
  - Result: LaunchAgent is `state = running`
- `GET http://127.0.0.1:4329/`
  - Result: success
- `GET http://127.0.0.1:4329/api/snapshot`
  - Result: success

## Notes

- This is now a user-level background service, not a chat-session-only foreground process.
- The plist and logs stay inside the repository; no files were written to `~`.

# T35 Result

## Outcome

- Fixed the table-column clipping issue shown in the screenshot.
- Applied three safeguards:
  - horizontal scrollbar support in `ScrollArea`
  - fixed table layout + explicit column widths in cost/pricing tables
  - nowrap numeric value cells and break-word model/project cells

## Validation

- `npm test` -> 28 passing

## Notes

- This change targets visibility/readability first: right-side rate/cost columns remain accessible instead of being visually cut off.

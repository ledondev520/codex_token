# T11 Results

## Outcome

The billing tab was redesigned to improve readability and filtering:

- Added `累计 / 自然月` switching for fee and token totals.
- Replaced the ambiguous chart shell with a clearer dual-axis line chart.
- Added quick range presets and draggable range sliders for fast time-window selection.
- Replaced the ledger list with a table that has visible headers.
- Deduplicated the grouped pricing display so `GPT-5.4` only appears once.

## Verification

- `npm test`
  - Result: 9 passing tests on 2026-03-08 19:11 Asia/Shanghai
- Browser smoke test on `http://127.0.0.1:4326`
  - Verified `自然月` changes the billing totals
  - Verified range sliders update the selected date label
  - Verified ledger headers `日期 / Token / 费用`
  - Verified pricing table shows `GPT-5.4` once

## Notes

- The time-range control is implemented as dual draggable sliders plus preset chips, which is more robust in a static local dashboard than a custom canvas brush.

# T25 Result

## Outcome

The migrated React frontend is now part of the runtime startup contract, not just the development workflow.

Changes:

- `npm start` now rebuilds the Vite client before launching the Node server
- `npm dev` now rebuilds before starting the watched server
- `scripts/run-dashboard.sh` now rebuilds before launchd starts the dashboard on port `4329`
- README and checkpoint files now describe `src/` as the frontend source of truth and `public/` as build output

## Validation

- `npm test` -> 27 passing

## Notes

- This adds a new operational risk: if Vite build fails under launchd, the dashboard will not start. That is tracked as `R18` in `RISKS.md`.

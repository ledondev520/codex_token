# T18 Result

## Outcome

The local Codex dashboard remains on the Mac and is now reachable through the VPS without copying `~/.codex` to the server.

```text
phone -> 23.81.118.51:18080 -> nginx on VPS -> 127.0.0.1:14329 on VPS -> SSH reverse tunnel -> 127.0.0.1:4329 on Mac
```

## Deliverables

- VPS nginx reverse proxy on public port `18080`
- SSH key-based reverse tunnel from the Mac to the VPS
- `launchd/com.helena.codex-usage-dashboard-vps-preview.plist`
- `scripts/run-vps-preview-tunnel.sh`
- README operational notes for the VPS preview path

## Validation

- `curl http://127.0.0.1:4329/`
- `ssh root@23.81.118.51 curl http://127.0.0.1:14329/`
- `curl -u <preview credentials> http://23.81.118.51:18080/`

## Notes

- The preview currently uses a raw VPS IP and HTTP, so credentials are not transport-encrypted.
- Moving to a domain and HTTPS is the next hardening step.
